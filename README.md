# Next.js + Express bulletin-board sample created by GPT4
- Runs on Windows.
- Please install the mongoDB. And create a DB named bulletin_board.
- Run `npm install` on frontend and backend
- Execute the run_bbs.bat.